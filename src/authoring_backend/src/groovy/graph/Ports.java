package groovy.graph;

public enum Ports {
    FLOW_IN,
    FLOW_OUT,
    IF_PREDICATE,
    IF_BODY,
    FOREACH_LIST,
    FOREACH_LOOPVAR,
    FOREACH_BODY,
    ASSIGN_LHS,
    ASSIGN_RHS,
    A,
    B
}
