package authoring_backend.src.groovy.graph;

public enum Ports {
    FLOW_OUT,
    IF_PREDICATE,
    IF_BODY,
    FOR_INIT,
    FOR_CHECK,
    FOR_MUTATE,
    FOR_BODY,
    ASSIGN_RHS,
    A,
    B
}
